# Figure captions for v8 mixed-augmentation experiments

Draft date: 2026-07-07

## Fig. 1 Simulation and validation framework

Files:

```text
writing/figures/fig1_gated_imaging_framework.png
writing/figures/fig1_gated_imaging_framework.pdf
```

Suggested caption:

Figure 1. Overview of the proposed simulation and validation framework for gated-imaging-based discrimination between 3D targets and planar false targets. Selected military 3D models are rendered in Blender with depth and reflectance information, followed by a rectangular laser-gate response model to generate true 3D gate stacks. Planar false targets are generated by camera-depth flattening while preserving the visible silhouette, and the resulting gate stacks are evaluated with anti-shortcut controls, single-gate ablations, and independent clean/noisy tests.

Chinese speaking note:

这张图用于讲清楚论文主线：不是单纯训练一个分类网络，而是先构建物理可解释的选通仿真，再构造平面假目标，最后用反捷径实验验证 full gate stack 是否真的有贡献。

## Fig. 2 Rectangular pulse-gate overlap response

Files:

```text
writing/figures/fig2_rectangular_overlap_response.png
writing/figures/fig2_rectangular_overlap_response.pdf
```

Suggested caption:

Figure 2. Rectangular pulse-gate overlap model used in the gated-image simulation. (A) The normalized received response is determined by the temporal overlap length between the transmitted laser pulse and the receiver gate. Equal pulse/gate widths produce a triangular response, whereas the v8 unequal-width setting produces a trapezoidal response with a plateau. (B) A planar false target samples the same whole-object response across gates, while a true 3D target integrates responses from multiple visible depths.

Chinese speaking note:

这张图回答“假目标是不是线性衰减”的问题：不是任意线性衰减。矩形激光函数和矩形门函数相乘积分后，本质是重叠长度；等宽时近似三角形，不等宽时是梯形。平面假目标只是这个响应曲线在三个 gate 上的采样。

## Fig. 3 Example gate stacks for a true 3D target and a planar false target

Files:

```text
writing/figures/fig3_true3d_flatfalse_gate_examples.png
writing/figures/fig3_true3d_flatfalse_gate_examples.pdf
```

Source:

```text
dataset_new/Military_TrueFalse_Selected44_blender_refl_overlap_w15_m012_v8
sample_id = 02_Fighter_Jet__02_Fighter_Jet_02_Fighter_Jet_044_95fe1170
```

Suggested caption:

Figure 3. Representative raw gate-stack images generated by the v8 rectangular-overlap simulation. The true 3D target changes its visible structure across gates because different depth regions contribute to different range windows. In contrast, the planar false target preserves the complete object silhouette across gates, with intensity changes governed by the pulse-gate overlap response.

Chinese speaking note:

这张图要强调：真实三维目标在 gate0 中只剩机身深度切片，而 gate1/gate2 包含更完整结构；平面假目标从头到尾都是完整飞机轮廓，只是整体亮度随 gate 响应变化。这正是当前仿真比早期“只出现一部分假目标”的版本更合理的地方。

## Fig. 4 Scalar shortcut control

Files:

```text
writing/figures/fig4_scalar_shortcut_control.png
writing/figures/fig4_scalar_shortcut_control.pdf
```

Suggested caption:

Figure 4. Strongest single-gate scalar shortcut before and after per-gate maximum normalization. In the raw v8 data, gate 2 contains a strong p99 intensity shortcut with a threshold-classification accuracy of 0.9886. After per-gate maximum normalization, the strongest scalar shortcut is reduced to 0.7955 and shifts mainly toward edge/shape statistics, indicating that absolute brightness is no longer the dominant cue.

Chinese speaking note:

这张图是论文“反捷径验证”的核心证据。不能只说网络准确率高，必须先证明原始数据里有什么捷径，再说明我们如何削弱捷径。这里最关键的是 gate2 的 p99 从接近 0.99 的阈值分类准确率降到约 0.78，说明原始强度线索被明显压低。

## Fig. 5 Full gate stack versus single-gate inputs

Files:

```text
writing/figures/fig5_full_stack_vs_single_gate_robustness.png
writing/figures/fig5_full_stack_vs_single_gate_robustness.pdf
```

Suggested caption:

Figure 5. Independent evaluation of the full three-gate stack and single-gate baselines under clean, light-noise, and strong-noise conditions. Bars denote mean accuracy over three random seeds (42/332/2026), and error bars denote standard deviation. The full three-gate stack gives the highest mean accuracy in all three evaluation conditions, supporting the usefulness of multi-gate information beyond a single selected range slice.

Chinese speaking note:

这张图用于说明“选通序列本身有价值”。如果只看某一个 gate，模型仍然可能得到不错结果，特别是 gate0 和 gate2；但在三种独立测试条件下，完整三 gate stack 的平均准确率最高，说明网络不是完全依靠某一张图的单帧捷径。

## Fig. 6 Fusion-mode comparison for the full gate stack

Files:

```text
writing/figures/fig6_full_stack_fusion_robustness.png
writing/figures/fig6_full_stack_fusion_robustness.pdf
```

Suggested caption:

Figure 6. Independent evaluation of three full-stack fusion strategies under clean, light-noise, and strong-noise conditions. Bars denote mean accuracy over three random seeds, and error bars denote standard deviation. The attention-residual head provides the best clean-condition performance, mean fusion is slightly better under light noise, while attention fusion is more robust under strong noise. This indicates that gate-stack fusion is a design variable rather than a solved component.

Chinese speaking note:

这张图不要讲成“attention_residual 全面最好”。更合理的讲法是：attention_residual 在验证集和干净测试中最好，但强噪声下 attention 更稳。这说明当前网络是一个有效电子基线，后续如果要做多模光神经网络，可以把 gate 融合作为重点优化模块。

## Fig. 7 Four-view full gate stack versus single-gate inputs

Files:

```text
writing/figures/fig7_mv4_full_stack_vs_single_gate_robustness.png
writing/figures/fig7_mv4_full_stack_vs_single_gate_robustness.pdf
```

Suggested caption:

Figure 7. Four-view v8 validation of the full three-gate stack and single-gate baselines under clean, light-noise, and strong-noise conditions. Each source model is rendered at four yaw rotations (0, 90, 180, and 270 degrees), and the validation split keeps all views of the same source model in the same partition. Bars denote mean accuracy over three random seeds, and error bars denote standard deviation. The full gate stack remains the best mean performer in all three evaluation conditions, with a larger margin under strong noise.

Speaking note:

This is the current strongest ablation result. Fig. 5 proves the gate-stack advantage on the original single-view dataset. Fig. 7 repeats the same question after adding four yaw views and preventing cross-view leakage. The result supports the claim that the full gate stack adds useful depth-gated evidence, but it still does not prove real-world deployment robustness.

## Fig. 8 Four-view full-stack fusion comparison

Files:

```text
writing/figures/fig8_mv4_full_stack_fusion_robustness.png
writing/figures/fig8_mv4_full_stack_fusion_robustness.pdf
```

Suggested caption:

Figure 8. Four-view v8 validation of full-stack fusion modes under clean, light-noise, and strong-noise conditions. Bars denote mean accuracy over three random seeds, and error bars denote standard deviation. The three fusion modes are tied under clean evaluation. Mean fusion is slightly better under light noise, while simple attention is clearly more robust under strong noise. This result indicates that the fusion head is an engineering variable rather than the central contribution of the study.

Speaking note:

This figure answers the question of whether the attention-residual module is the main innovation. It is not. After four-view validation, attention-residual is not consistently best and degrades under strong noise. The stronger paper story is the physical simulation and anti-shortcut validation framework, with the neural network used as a lightweight baseline.

## Fig. 9 Hard-nuisance failure boundary

Files:

```text
writing/figures/fig9_hard_nuisance_failure_boundary.png
writing/figures/fig9_hard_nuisance_failure_boundary.pdf
```

Suggested caption:

Figure 9. Failure-boundary evaluation under structured nuisance shifts. The four-view v8 attention models are trained on the normalized clean/noisy dataset and evaluated on clean, light-noise, strong-noise, and hard-nuisance datasets. The hard-nuisance datasets introduce low-frequency reflectance texture, weak background scatter, and partial occlusion while preserving per-sample maximum intensity. The full gate stack remains robust under the original clean/noisy protocol but collapses to approximately chance accuracy under structured nuisance shifts, indicating that nuisance-aware simulation and training are required before making realistic robustness claims.

Speaking note:

This is not a bad result to hide. It is the boundary of the current method. It says: our gate-stack physics and anti-shortcut validation work under controlled v8 simulation, but realistic structured variations still break the trained model. This gives the next innovation direction and prevents overclaiming.

## Fig. 10 Domain-randomized training strategy comparison

Files:

```text
writing/figures/fig10_domain_randomization_strategy_comparison.png
writing/figures/fig10_domain_randomization_strategy_comparison.pdf
```

Suggested caption:

Figure 10. Training strategy comparison under clean/noisy evaluation and structured hard-nuisance shifts. Bars denote mean grouped-validation accuracy over three random seeds, and error bars denote standard deviation. The normal mixed-noise baseline performs best under clean and light-noise conditions but collapses to chance accuracy under the hard-nuisance datasets. Online structured nuisance augmentation does not recover hard-nuisance performance in the current implementation and weakens strong-noise robustness. In contrast, explicit domain mixing between the normal and hard-mild simulated domains, combined with strong-noise augmentation, recovers hard-mild accuracy to 0.9394 and hard-strong accuracy to 0.7727 while preserving 0.9091 strong-noise accuracy.

Speaking note:

This figure is the current strongest "innovation upgrade" result. Fig. 9 shows the failure boundary; Fig. 10 shows a concrete fix direction. The correct wording is not "the model is robust in real scenes", but "when structured nuisance factors are included as part of the simulation domain, the robustness boundary improves substantially compared with clean/noisy-only training."

## Paper-safe conclusion from these figures

The current evidence supports this claim:

```text
In a controlled Blender-based gated-imaging simulation, the complete three-gate sequence provides more stable discriminative information than any individual gate for separating 3D targets from planar false targets. This remains true after four-view yaw expansion with model-level grouped validation. Structured nuisance shifts expose a clear failure boundary, but explicit domain-randomized simulation improves this boundary while preserving strong-noise performance. However, the optimal fusion strategy remains condition-dependent, and real-world generalization still requires broader viewpoint, material, background, and sensor validation.
```

Avoid this claim:

```text
The proposed network has solved robust military target recognition or universally detects all false targets.
```

## Fig. 11 Domain-mixed full gate stack versus single-gate baselines

Files:

```text
writing/figures/fig11_domainmix_full_stack_vs_single_gate.png
writing/figures/fig11_domainmix_full_stack_vs_single_gate.pdf
```

Suggested caption:

Figure 11. Domain-mixed full-stack versus single-gate ablation under clean, light-noise, strong-noise, hard-mild, and hard-strong evaluation. All models are trained on the normal-plus-hard-mild mixed domain with strong-noise augmentation and evaluated with model-level grouped splits over three seeds. The full three-gate stack is the best mean performer under all five conditions, indicating that the domain-randomized robustness improvement is not reproduced by any individual gate alone.

Speaking note:

This is the final control experiment for the current paper story. Fig. 10 shows that domain mixing improves the hard-nuisance boundary. Fig. 11 shows that this improvement is strongest when the model uses the full gate stack, not one isolated gate. The correct claim remains simulation-limited: the result supports gate-stack value under the current controlled domain-randomized simulation, not real-world deployment robustness.
